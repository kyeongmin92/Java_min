package com.hk.app;

public class Card { // 트럼프카드 
	// 속성
	int number; // 1 2 3 4  // iv (인스턴스 변수)
	String kind; // 하트 "h", 다이아몬드"d", 스페이스"s", 클로버"c"  // iv
	// 동작(행동)
	static int height; // cv (클래스 변수)
	static int width;  // cv
	
	// 행동, 동작
	// { 변수 }  // lv (지역변수)

}

// main
// Card h1 = new Card();
// Card d1 = new Card();